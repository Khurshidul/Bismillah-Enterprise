import React from "react";

interface PropTypes {}

const Secret: React.FC<PropTypes> = () => {
    return (
        <div>
            <h2> Secret route</h2>
        </div>
    );
};

export default Secret;