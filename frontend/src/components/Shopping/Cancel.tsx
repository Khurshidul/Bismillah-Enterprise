import React from "react";

interface PropTypes {}

const Cancel: React.FC<PropTypes> = () => {
    return (
        <div>
            <h1>some thing is wrong. Please try again...</h1>
        </div>
    );
};

export default Cancel;